spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true
      hibernate:
        default_batch_fetch_size: 100
        format_sql: true
     #   dialect: org.hibernate.dialect.MySQL5Dialect

